# Interface reference

`components.html` is an integration fragment, not a complete website or approved marketing copy. Load Lora and DM Sans, then `../tokens/tokens.css` and `reference.css` in the consuming page. Resolve image URLs relative to the final page location. See [implementation guidance](../docs/IMPLEMENTATION.md).
